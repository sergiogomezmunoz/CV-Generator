---
// Index page

// Description ---------------------------------------------------------------------------------------
// This is the main page of the project. It is the first page that will be displayed when the project
// is accessed. This page is used to display information about the project and provide links to other
// pages or external resources. The page is built using Astro components and layout. The layout
// component is used to define the structure of the page, while the card component is used to display
// links to other resources. The page also includes some basic styling to make it visually appealing.
// ---------------------------------------------------------------------------------------------------

// Imports -------------------------------------------------------------------------------------------
import About from "@/components/About.astro"; // Import the about component
import Education from "@/components/Education.astro"; // Import the education component
import Experience from "@/components/Experience.astro"; // Import the experience component
import Hero from "@/components/Hero.astro"; // Import the hero component
import Layout from "@/layouts/Layout.astro"; // Import the layout
import Projects from "@/components/Projects.astro"; // Import the projects component
import Skills from "@/components/Skills.astro"; // Import the skills component
// ---------------------------------------------------------------------------------------------------
---

<Layout title="CV Generator">
  <main>
    <Hero />
    <About />
    <Experience />
    <Education />
    <Projects />
    <Skills />
  </main>
</Layout>
